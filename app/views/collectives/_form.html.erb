<%= form_with(model: collective, local: true) do |form| %>
  <% if collective.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collective.errors.count, "error") %> prohibited this collective from being saved:</h2>

      <ul>
      <% collective.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :created %>
    <%= form.date_select :created %>
  </div>

  <div class="field">
    <%= form.label :broke_up %>
    <%= form.date_select :broke_up %>
  </div>

  <div class="field">
    <%= form.label :bio %>
    <%= form.text_area :bio %>
  </div>

  <div class="field">
    <%= form.label :style %>
    <%= form.text_field :style %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
